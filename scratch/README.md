# scratch/

Exploratory throwaway scripts kept for provenance — **not** part of the experiment or its
reproduce path. They predate the `src/config.py` refactor and may contain hardcoded local
connection strings. The canonical entry points are `simulate.py` and `plot_results.py` in the
repo root.
