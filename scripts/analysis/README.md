# Analysis scripts

Gate calibration, guardrail checks, ablations, and plotting.
These scripts support the experiment narrative in `docs/` — they are not
entry points for running experiments (see `simulate.py`, `run_flip_experiment.py`,
and `run_shift_experiment.py` in the repo root).
